<%- include('../layouts/header', {title: 'All listings'}) %>
<html>

<body>

  <h2 class="w-full text-2xl text-center mx-auto py-4">All Listings</h2>
  <section class="flex flex-wrap justify-center align-middle gap-4 text-gray-600 mx-auto w-full">

    <% for( let list of listings ) { %>
    <% const {_id, title, price, image} = list %>
    <div class="cardd w-2/3 md:w-1/3 lg:w-1/4 border rounded">
      <div class="upper hover:opacity-75 opacity-100">
        <a href="/listings/<%= _id %>"><img class="rounded-t cover aspect-video" src="<%= image %>" alt="" srcset="" /></a>
      </div>
      <div class="bottom px-3 py-4">
        <p class="text-xl md:text-base font-semibold"><%= title %></p>
        <p class="md:text-sm">₹<%= price.toLocaleString('en-IN') %>/night</p>
      </div>
    </div>
    <% } %>

  </section>
  <%- include('../layouts/footer') %>
</body>

</html>