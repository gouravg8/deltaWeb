// import { useState } from "react";
// import ReRender from "./ReRender";

function Count() {
  //   let [like, setLike] = useState("regular");
  // const [isLike, setIsLike] = useState(false);
  //   way one
  //   function handleLike() {
  //     like == "regular" ? setLike("solid") : setLike("regular");
  //   }

  //   way2
  // function handleLike() {
  //   setIsLike(!isLike);
  // }
  return (
    <>
      {/* <p
        className="my-12 text-3xl text-red-500 cursor-pointer"
        onClick={handleLike}
      > */}
      {/* <i
          className={`fa-${like} fa-heart cursor-pointer text-red-600`}
          onClick={handleLike}
        ></i> */}
      {/* {isLike ? (
          <i className="fa-solid fa-heart"></i>
        ) : (
          <i className="fa-regular fa-heart"></i>
        )}
      </p> */}
      {/* <ReRender /> */}
    </>
  );
}

export default Count;
